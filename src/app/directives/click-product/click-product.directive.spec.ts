import { ClickProductDirective } from './click-product.directive';

describe('ClickProductDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickProductDirective();
    expect(directive).toBeTruthy();
  });
});
